title: $:/plugins/danielo515/tiddlypouch/config/skinny-tiddlers-view

function(doc){
    var fields = {};  
    for(var field in doc.fields ){ 
        if( ['text','title'].indexOf(field) === -1){
            fields[field] = doc.fields[field];
        }
    }
    fields.revision = doc._rev;
    emit(doc._id,fields);
 }